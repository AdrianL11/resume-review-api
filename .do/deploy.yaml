spec:
  name: resume-review-api
  services:
    - name: web
      git:
        branch: master
        repo_clone_url: https://github.com/AdrianL11/resume-review-api.git
      dockerfile_path: Dockerfile
